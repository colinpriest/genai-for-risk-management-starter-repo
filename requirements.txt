# Tested with the versions in constraints.txt. Lower bounds alone were not reproducible:
# `include_groups=` on a groupby-apply needs pandas 2.2, and the code declared >=2.0.
pandas>=2.2,<3
numpy>=1.26,<3
scikit-learn>=1.5,<1.8
scipy>=1.11,<2
statsmodels>=0.14,<0.16
openai>=1.40,<2
# The UNSW proxy speaks the Responses API only, so structured output goes through
# instructor's RESPONSES_TOOLS mode. src/unsw_ai.py imports it at module scope, which
# means Words, Replay and Shock will not even IMPORT without it - this is not optional.
instructor>=1.12,<2
httpx>=0.27,<0.29
tenacity>=8.2,<10
pydantic>=2.5,<3
python-dotenv>=1.0,<2
pyarrow>=14.0
openpyxl>=3.1
xlrd>=2.0
matplotlib>=3.8,<4
joblib>=1.3,<2
pypdf>=4.0,<6
beautifulsoup4>=4.12,<5
pytest>=7.4,<9

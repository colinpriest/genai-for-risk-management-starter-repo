# Tested on Python 3.12 (Windows 11, macOS 14, Ubuntu 22.04).
#
# Lower bounds alone let two team members install different versions and get different
# numbers, which is a reproducibility problem you cannot see. These are the versions the
# starter was tested against. Pin them unless you have a reason not to, and if you change one,
# record it in your report.
#
# For an exact lock:  pip freeze > requirements-lock.txt   and commit that too.

pandas==2.3.3
numpy==2.1.3
statsmodels==0.14.6
plotly==6.2.0
pydantic==2.10.3
openai==1.109.1
sentence-transformers==3.3.1
scikit-learn==1.6.0
yfinance==0.2.66
pandas-datareader==0.10.0
requests==2.32.5
beautifulsoup4==4.12.3
lxml==5.4.0
python-dotenv==1.0.1
pytest==7.4.4
pyarrow==20.0.0
pypdf==5.9.0
xlrd==2.0.2                # legacy .xls, required to read the supplied GPR workbook
